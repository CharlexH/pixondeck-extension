# Public client configuration only. Never add a Clerk secret or model API key.
# Defaults build a shell; live sign-in needs an authorized Clerk/API environment.
POD_API_ORIGIN=http://localhost:8787
POD_SITE_ORIGIN=http://localhost:3000
POD_CLERK_PUBLISHABLE_KEY=
POD_CLERK_FRONTEND_API=
POD_CLERK_SYNC_HOST=http://localhost
# Optional PUBLIC extension key; a local identity is generated when absent.
POD_EXTENSION_PUBLIC_KEY=
