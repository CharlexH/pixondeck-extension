# Screenshot provenance

Captured from the repository's local fixture preview at a 460 × 760 CSS-pixel viewport to represent the extension side panel. The client UI is unmodified. The sample prompt was edited through the UI, favorites use the fixture API, and BYOK credentials are empty. Images use the bundled PixOnDeck icon. These are illustrative demo states, not real account activity or model benchmarks.

- workspace.png: editable prompt and recent tasks.
- saved-prompts.png: saved prompt view.
- byok-settings.png: provider configuration with no API key entered.
